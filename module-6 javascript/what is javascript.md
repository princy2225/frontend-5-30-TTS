What is javascript?
 
  javascript is a client side scripting language.
  javascript is load on web browser.
  javascript developed by ECMScript organization.
  javascript is load in <body> and <head> tag both using <script> tag
  javascript used as programming language.
    a) compiler
    b) interpreter (js is inp based lauguage)
    Note: interpreter is used convert high to low binary language (0101)

  javascript is save with .js or extension is .js
  javascript is most popular language.
  javascript is open source. (not need a subscription)
  javascript is support on cross-plateform(support all OS) operating system
  ex: windows family , linux , ios , android os etc.
  javascript is case-sensitive language.

  Example: document.getElementById("");
  javascript debug in console.
  ex: console.log("hi i m princy")

  javascript output method:
   1) alert()                                                  [print in box]
   2) document.write()                                         [print in document]
   3) console.log                                              [\n for new line on content]
   4) terminal                                                 (node js)
   5) REPL (Read | Evaluate | Print | Loop)                    [cmd main file me jake node use for refer or read use or your work refer]
   6) window.print()                                           [+"<br>"start from new line content]
   7) confirm()                                                [confirm is used to check confirmations from users]
   8) prompt()                                                 [prompt is used to take input form users and create a label of users]
   9) document.writeln()                                       [line me print krta hai]
   10) document.getElementById()           [DOM is stands for document object Modal, DOM is used to access html element inside of javascript via DOM]
   EX: document.getElementById()


   javascript three way to run:
   1) head
   2) body
   3) external javascript [.js file browser pr load nahi hoti html me uska path dal k output la sakte hai]

 What is operator in javascript?

   operater is operate some action i.e(that is) addition | substraction | division | comparision etc.

   Types of operater;
   There are some types operator                          
   a) arithmetic operator
   ex: + , - , * , / , % etc                             

   b) comparison operater
   ex: >, >= , < , <= , != etc

   c) assignment operater
   ex: = , == 

   d) logical operator
   ex: && , ||

   e) ternary operator
   ex: 

   f) bitwise operator
    ex: &(and) , |(or) , ~ (not), ^(xor) , 




 What is javascript variables?
   js variables is just like container where we stored an information about data i.e called variables
   or 
   js stored information about data i.e called variables

   Note: js defined variables via var | let | const keyword
   ex:
   var a=10;
   const a=10;
   let a=10;

   var: var is defined a variables , var is used to re-assigned and  redeclared data ,EX: var a=10; var b=10; console.log(a); console.log(b)

   let: let is also define variables. let is used to re-assigned but can not be redeclared data EX: let a=10; b=10;[work] a=10 a=20 [error a doble]

  const: const is also defined a variables , const is used to neither re-assigned or neither redeclared data can not be changed
   ex: const a=10; [one value, unique id, password]
  